import React from 'react';

const ProductsPage = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        marginTop: '15rem',
        fontSize: '4rem',
      }}
    >
      Products Page
    </div>
  );
};

export default ProductsPage;
