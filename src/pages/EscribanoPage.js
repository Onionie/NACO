import React from 'react';

const EscribanoPage = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        marginTop: '15rem',
        fontSize: '4rem',
      }}
    >
      Escribano Page
    </div>
  );
};

export default EscribanoPage;
